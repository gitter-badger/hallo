@keyframes kf-input-on {
  0% { top: 0; }
  100% { top: sizeEyebrow*0.55; }
}
@keyframes kf-input-off {
  0% { top: sizeEyebrow*0.55; }
  100% { top: 0; }
}

input[type="text"]
  position: relative
  top: 0.375rem // 6px
  left: 0
  border: none
  width: 100%
  font-size: 1rem
